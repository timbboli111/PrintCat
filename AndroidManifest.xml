<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.printcat.app">

    <!-- Permission dasar Fyne -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth Classic (Android <= 11) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Bluetooth Classic (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Fitur Bluetooth tidak wajib -->
    <uses-feature android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:label="PrintCat"
        android:debuggable="false">
        <activity
            android:name="org.golang.app.GoNativeActivity"
            android:label="PrintCat"
            android:configChanges="0xa0">
            <meta-data
                android:name="android.app.lib_name"
                android:value="printcat" />
        </activity>
    </application>

</manifest>